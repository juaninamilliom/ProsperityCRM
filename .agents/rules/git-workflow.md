# Git Workflow Rules

- **NEVER PUSH DIRECTLY TO `main`**.
- Always create a descriptive feature or bugfix branch (e.g., `feat/...` or `fix/...`).
- Push to the feature branch and open a Pull Request using `gh pr create`.
- Wait for the user to review and merge the PR.
